
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "linear_rational_wishart"
version = "0.1.0"
description = "Linear Rational Wishart models for FX pricing"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "scipy",
    # add other dependencies
]

[project.optional-dependencies]
dev = ["pytest", "jupyter"]